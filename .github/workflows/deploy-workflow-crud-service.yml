name: Deploy Workflow CRUD Service

on:
  push:
    branches: [master, main]
    paths:
      - 'services/workflow-crud-service/**'
      - 'scripts/deploy-workflow-crud-service.sh'
      - 'scripts/ctrlchecks-workflow-crud-service.service'
      - '.github/workflows/deploy-workflow-crud-service.yml'
  workflow_dispatch:
    inputs:
      force:
        description: 'Force deploy even without path changes'
        required: false
        default: 'false'

concurrency:
  group: deploy-workflow-crud-service
  cancel-in-progress: false

jobs:
  test:
    name: Type-check & test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/workflow-crud-service
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: services/workflow-crud-service/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm run type-check

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Upload dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: workflow-crud-service-dist
          path: |
            services/workflow-crud-service/dist/
            services/workflow-crud-service/package.json
            services/workflow-crud-service/package-lock.json
          retention-days: 1

  deploy:
    name: Deploy to EC2
    needs: test
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: workflow-crud-service-dist
          path: services/workflow-crud-service/

      - name: Write SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_PRIVATE_KEY }}" > ~/.ssh/ctrlchecks.pem
          chmod 600 ~/.ssh/ctrlchecks.pem
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Package and upload
        run: |
          tar -czf /tmp/workflow-crud-service-deploy.tar.gz \
            -C services/workflow-crud-service \
            dist/ package.json package-lock.json
          scp -i ~/.ssh/ctrlchecks.pem \
            /tmp/workflow-crud-service-deploy.tar.gz \
            ubuntu@${{ secrets.EC2_HOST }}:/tmp/workflow-crud-service-deploy.tar.gz
          scp -i ~/.ssh/ctrlchecks.pem \
            scripts/ctrlchecks-workflow-crud-service.service \
            ubuntu@${{ secrets.EC2_HOST }}:/tmp/ctrlchecks-workflow-crud-service.service

      - name: Extract, install, restart
        run: |
          ssh -i ~/.ssh/ctrlchecks.pem ubuntu@${{ secrets.EC2_HOST }} bash << 'REMOTE'
            set -euo pipefail
            TARGET=/opt/ctrlchecks-workflow-crud-service

            # Update systemd unit if changed
            if ! diff -q /tmp/ctrlchecks-workflow-crud-service.service \
                 /etc/systemd/system/ctrlchecks-workflow-crud-service.service &>/dev/null 2>&1; then
              sudo cp /tmp/ctrlchecks-workflow-crud-service.service \
                      /etc/systemd/system/ctrlchecks-workflow-crud-service.service
              sudo systemctl daemon-reload
              echo "systemd unit updated"
            fi
            rm -f /tmp/ctrlchecks-workflow-crud-service.service

            tar -xzf /tmp/workflow-crud-service-deploy.tar.gz -C "$TARGET"
            rm /tmp/workflow-crud-service-deploy.tar.gz

            cd "$TARGET"
            npm ci --omit=dev
            sudo systemctl restart ctrlchecks-workflow-crud-service
            sleep 5

            curl -fsS http://localhost:3007/health/live \
              && echo "health/live OK" \
              || (sudo journalctl -u ctrlchecks-workflow-crud-service -n 30 --no-pager && exit 1)
            curl -fsS http://localhost:3007/health/ready \
              && echo "health/ready OK" \
              || exit 1
          REMOTE

      - name: Clean up SSH key
        if: always()
        run: rm -f ~/.ssh/ctrlchecks.pem

# Required GitHub Secrets:
#   EC2_SSH_PRIVATE_KEY — contents of ctrlchecks-backend.pem
#   EC2_HOST            — 3.7.115.58 (or the instance's domain)
