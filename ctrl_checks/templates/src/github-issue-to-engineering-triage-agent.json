{
  "id": "93000000-0000-4000-8000-000000000015",
  "name": "GitHub Issue to Engineering Triage Agent",
  "description": "Receive GitHub issue webhooks, classify severity and category, log the triage in PostgreSQL, and alert engineering.",
  "category": "AI Agent",
  "nodes": [
    {
      "id": "webhook_intake",
      "type": "custom",
      "position": {
        "x": 100,
        "y": 300
      },
      "data": {
        "icon": "Box",
        "type": "webhook",
        "label": "Receive Event Payload",
        "category": "triggers",
        "config": {
          "path": "/github-issue-to-engineering-triage-agent",
          "httpMethod": "POST",
          "responseMode": "onReceived"
        },
        "notes": {
          "family": "Triggers",
          "what": "Starts your workflow the moment another app sends it data — like a doorbell for your automation",
          "overview": "Starts a workflow when an external app, website form, payment system, or internal service sends data to the generated CtrlChecks webhook URL. Captures the request body, headers, query values, and method for the next node.",
          "tips": [],
          "docsHref": "/docs/nodes/webhook",
          "why": "Receives one normalized payload from the source system through an allowed webhook trigger."
        }
      }
    },
    {
      "id": "tool_related_issues",
      "type": "custom",
      "position": {
        "x": 420,
        "y": 300
      },
      "data": {
        "icon": "Globe",
        "type": "http_request",
        "label": "Fetch Related Issue Context",
        "category": "http_api",
        "config": {
          "url": "{{input.relatedIssuesUrl}}",
          "method": "GET",
          "timeout": 10000
        },
        "notes": {
          "family": "HTTP & API",
          "what": "Calls any web URL and gets data back — your universal connector to the internet",
          "overview": "Call an external API or webhook, optionally adding headers, query parameters, body data, and a timeout.",
          "tips": [],
          "docsHref": "/docs/nodes/http_request",
          "why": "Avoids blocked GitHub Trigger and avoids a direct issue-tracker write while still letting the agent inspect related issue context."
        }
      }
    },
    {
      "id": "ai_agent",
      "type": "custom",
      "position": {
        "x": 740,
        "y": 300
      },
      "data": {
        "icon": "Bot",
        "type": "ai_agent",
        "label": "AI Agent",
        "category": "ai",
        "config": {
          "model": "gemini-3.5-flash",
          "temperature": 0.2,
          "userInput": "Process this workflow input and any attached tool results: {{input}}",
          "systemPrompt": "Classify this issue as bug, feature, docs, or question with priority critical/high/medium/low and a one-line engineering summary. Return ONLY JSON with the fields named in this workflow.",
          "outputFormat": "json",
          "memoryScope": "none",
          "requireApprovalFor": "destructive"
        },
        "notes": {
          "family": "AI",
          "what": "An autonomous AI brain that reasons, makes decisions, and uses tools on your behalf",
          "overview": "Runs one prompt-driven AI step. The runtime extracts userInput from this node or upstream message-like fields, infers the provider from Model, calls the LLM adapter, and packages response_text plus optional structured fields.",
          "tips": [],
          "docsHref": "/docs/nodes/ai_agent",
          "why": "Applies the source AI-agent prompt to the current request and attached read-side context, returning structured JSON for deterministic downstream steps."
        }
      }
    },
    {
      "id": "js_parse_agent",
      "type": "custom",
      "position": {
        "x": 1060,
        "y": 300
      },
      "data": {
        "icon": "Code",
        "type": "javascript",
        "label": "Parse Agent JSON",
        "category": "data",
        "config": {
          "code": "const raw = input.response_text || input.output || input.response || input.text || input.content || \"\";\nlet parsed = {};\ntry {\n  const match = String(raw).match(/\\{[\\s\\S]*\\}/);\n  parsed = match ? JSON.parse(match[0]) : {};\n} catch (error) {\n  parsed = {};\n}\nconst message = parsed.message || parsed.reply || parsed.emailBody || parsed.body || parsed.summary || String(raw);\nreturn { ...input, ...parsed, responseText: String(raw), message };",
          "timeout": 5000
        },
        "notes": {
          "family": "Data",
          "what": "Writes and runs any JavaScript you like to transform or process your data",
          "overview": "Run sandboxed JavaScript once against the current workflow data and return the transformed value for downstream nodes.",
          "tips": [
            "Always return the value the next node should receive",
            "Default timeout is 5000ms and runtime caps at 30000ms",
            "Use Output Schema such as {\"type\":\"object\"} only as a top-level shape hint",
            "JavaScript has no credentials; connect downstream service node accounts separately",
            "Do not paste API keys, tokens, or passwords into code"
          ],
          "docsHref": "/docs/nodes/javascript",
          "why": "Turns the agent response into top-level fields before any branch or output node reads it."
        }
      }
    },
    {
      "id": "postgres_log_issue",
      "type": "custom",
      "position": {
        "x": 1380,
        "y": 300
      },
      "data": {
        "icon": "Database",
        "type": "postgresql",
        "label": "Log Issue Triage",
        "category": "data",
        "config": {
          "query": "INSERT INTO issue_triage (source, title, priority, category, summary, created_at) VALUES ('github', '{{input.title}}', '{{input.priority}}', '{{input.category}}', '{{input.summary}}', now())"
        },
        "notes": {
          "family": "Data",
          "what": "Connects to a PostgreSQL database to run queries or insert data",
          "overview": "Run PostgreSQL executeQuery, insert, update, or delete through the real database executor. The visual panel now uses runtime operation values and Parameters maps to the executor parameter array.",
          "tips": [
            "Use executeQuery for raw SQL with $1, $2 placeholders and Parameters as a JSON array.",
            "Insert returns inserted/count; update and delete return rows/rowsAffected because PostgreSQL uses RETURNING *.",
            "The backend schema still lists connectionString, but the executor validates host, username, password, database, port, and ssl.",
            "Update and delete require Where; the executor refuses missing where clauses.",
            "Store database passwords in Connections rather than ordinary workflow fields."
          ],
          "docsHref": "/docs/nodes/postgresql",
          "why": "Stores the triage result instead of creating Jira directly, matching the existing GitHub-trigger rework precedent."
        }
      }
    },
    {
      "id": "slack_issue_triage",
      "type": "custom",
      "position": {
        "x": 1700,
        "y": 300
      },
      "data": {
        "icon": "Hash",
        "type": "slack_message",
        "label": "Notify Engineering",
        "category": "output",
        "config": {
          "channel": "#engineering",
          "message": "Issue triage: {{input.priority}} / {{input.category}} - {{input.summary}}"
        },
        "notes": {
          "family": "Communication",
          "what": "Sends messages using a Slack app or bot connection",
          "overview": "Send Slack bot messages to channels, direct messages, or existing threads through a saved Slack OAuth2 connection. Use it for workplace alerts, support replies, incident updates, approvals, report summaries, and deployment notifications.",
          "tips": [
            "Select a Slack OAuth2 connection; do not paste bot tokens into message fields",
            "The Slack app needs chat:write, and the bot must be invited before it can post in private channels",
            "Use {{$json.channelId}} and {{$json.threadTs}} from Slack Trigger to reply in the same conversation",
            "Keep Message filled as readable fallback text even when Blocks contains a rich Block Kit layout",
            "Connect downstream service accounts separately; Slack OAuth2 only authorizes the Slack send"
          ],
          "docsHref": "/docs/nodes/slack_message",
          "why": "Alerts engineering with the classified issue summary."
        }
      }
    }
  ],
  "edges": [
    {
      "id": "e_1_webhook_intake_tool_related_issues",
      "source": "webhook_intake",
      "target": "tool_related_issues"
    },
    {
      "id": "e_2_tool_related_issues_ai_agent",
      "source": "tool_related_issues",
      "target": "ai_agent",
      "targetHandle": "tool"
    },
    {
      "id": "e_3_webhook_intake_ai_agent",
      "source": "webhook_intake",
      "target": "ai_agent",
      "targetHandle": "userInput"
    },
    {
      "id": "e_4_ai_agent_js_parse_agent",
      "source": "ai_agent",
      "target": "js_parse_agent"
    },
    {
      "id": "e_5_js_parse_agent_postgres_log_issue",
      "source": "js_parse_agent",
      "target": "postgres_log_issue"
    },
    {
      "id": "e_6_postgres_log_issue_slack_issue_triage",
      "source": "postgres_log_issue",
      "target": "slack_issue_triage"
    }
  ],
  "difficulty": "Intermediate",
  "estimated_setup_time": 20,
  "tags": [
    "github",
    "issues",
    "webhook",
    "postgresql",
    "slack"
  ],
  "is_featured": false,
  "is_active": true,
  "use_count": 0,
  "version": 2
}
