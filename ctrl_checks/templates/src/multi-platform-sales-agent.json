{
  "id": "93000000-0000-4000-8000-000000000011",
  "name": "Multi-Platform Sales Agent",
  "description": "Handle inbound sales payloads, use CRM and calendar context, log the conversation in Pipedrive, and notify sales.",
  "category": "AI Agent",
  "nodes": [
    {
      "id": "webhook_intake",
      "type": "custom",
      "position": {
        "x": 100,
        "y": 300
      },
      "data": {
        "icon": "Box",
        "type": "webhook",
        "label": "Receive Event Payload",
        "category": "triggers",
        "config": {
          "path": "/multi-platform-sales-agent",
          "httpMethod": "POST",
          "responseMode": "onReceived"
        },
        "notes": {
          "family": "Triggers",
          "what": "Starts your workflow the moment another app sends it data — like a doorbell for your automation",
          "overview": "Starts a workflow when an external app, website form, payment system, or internal service sends data to the generated CtrlChecks webhook URL. Captures the request body, headers, query values, and method for the next node.",
          "tips": [],
          "docsHref": "/docs/nodes/webhook",
          "why": "Receives one normalized payload from the source system through an allowed webhook trigger."
        }
      }
    },
    {
      "id": "memory_context",
      "type": "custom",
      "position": {
        "x": 420,
        "y": 110
      },
      "data": {
        "icon": "Brain",
        "type": "memory",
        "label": "Conversation Memory",
        "category": "ai",
        "config": {
          "sessionId": "{{input.contactId}}",
          "maxMessages": 20
        },
        "notes": {
          "family": "AI",
          "what": "Saves conversation history so your AI can remember things across multiple steps",
          "overview": "Passes sessionId, context, and incoming messages forward for AI workflows. The current runtime does not store, retrieve, clear, search, expire, or limit memory; those visible controls are legacy no-ops.",
          "tips": [
            "Use Session ID and Context as the real data passed to downstream AI steps.",
            "Store, Retrieve, Clear, and Search currently behave the same.",
            "TTL, Memory Type, and Max Messages are ignored by the current executor.",
            "No memory or searchResults field is produced."
          ],
          "docsHref": "/docs/nodes/memory",
          "why": "Provides the agent a stable session key and recent-context window for multi-turn conversations."
        }
      }
    },
    {
      "id": "tool_pipedrive_lookup",
      "type": "custom",
      "position": {
        "x": 420,
        "y": 300
      },
      "data": {
        "icon": "CircleDollarSign",
        "type": "pipedrive",
        "label": "Read Pipedrive Lead Context",
        "category": "crm",
        "config": {
          "resource": "deals",
          "operation": "search",
          "data": {
            "term": "{{input.email}}"
          }
        },
        "notes": {
          "family": "CRM",
          "what": "Manages deals, contacts, and activities in Pipedrive sales CRM",
          "overview": "Important: this node supports 11 real Pipedrive resources (person, organization, deal, activity, note, pipeline, stage, product, lead, file, webhook), each using its own individually-named ID and create/update fields - but the visual panel's generic Resource ID, Data (JSON), Search Term, and Fields inputs are never read by any of them. The real fields (like personId, dealTitle, searchTerm) must currently be set via workflow JSON or an AI-generated workflow. The \"Get Many\" dropdown option also never works - the real value is \"list\". Company Domain is fully decorative; the API client always calls the single global api.pipedrive.com address using only the API Token.",
          "tips": [],
          "docsHref": "/docs/nodes/pipedrive",
          "why": "Gives the agent CRM context before it drafts qualification and next steps."
        }
      }
    },
    {
      "id": "tool_calendar_slots",
      "type": "custom",
      "position": {
        "x": 420,
        "y": 490
      },
      "data": {
        "icon": "Calendar",
        "type": "google_calendar",
        "label": "Read Booking Availability",
        "category": "data",
        "config": {
          "resource": "event",
          "operation": "list",
          "calendarId": "primary",
          "maxResults": 10
        },
        "notes": {
          "family": "Data",
          "what": "Read, create, update, or delete events in your Google Calendar",
          "overview": "Create, list, update, or delete Google Calendar events. Manage your calendar programmatically. Runtime also supports get, quickAdd, and move for events, plus non-event resources, when set directly in workflow JSON.",
          "tips": [
            "Use \"primary\" for main calendar",
            "Times must be ISO 8601 format (UTC) â€” Start Time/End Time are converted automatically",
            "The new/updated event ID is returned as id, not eventId",
            "List returns events in the Time Min/Time Max window, with no date filtering by default"
          ],
          "docsHref": "/docs/nodes/google_calendar",
          "why": "Lets the agent offer realistic booking windows without using an unverified calendar substitute."
        }
      }
    },
    {
      "id": "ai_agent",
      "type": "custom",
      "position": {
        "x": 740,
        "y": 300
      },
      "data": {
        "icon": "Bot",
        "type": "ai_agent",
        "label": "AI Agent",
        "category": "ai",
        "config": {
          "model": "gemini-3.5-flash",
          "temperature": 0.2,
          "userInput": "Process this workflow input and any attached tool results: {{input}}",
          "systemPrompt": "You are a sales development rep. Answer product questions, qualify intent, and recommend booking only when the lead shows real intent. Return ONLY JSON with the fields named in this workflow.",
          "outputFormat": "json",
          "memoryScope": "conversation",
          "requireApprovalFor": "destructive"
        },
        "notes": {
          "family": "AI",
          "what": "An autonomous AI brain that reasons, makes decisions, and uses tools on your behalf",
          "overview": "Runs one prompt-driven AI step. The runtime extracts userInput from this node or upstream message-like fields, infers the provider from Model, calls the LLM adapter, and packages response_text plus optional structured fields.",
          "tips": [],
          "docsHref": "/docs/nodes/ai_agent",
          "why": "Applies the source AI-agent prompt to the current request and attached read-side context, returning structured JSON for deterministic downstream steps."
        }
      }
    },
    {
      "id": "js_parse_agent",
      "type": "custom",
      "position": {
        "x": 1060,
        "y": 300
      },
      "data": {
        "icon": "Code",
        "type": "javascript",
        "label": "Parse Agent JSON",
        "category": "data",
        "config": {
          "code": "const raw = input.response_text || input.output || input.response || input.text || input.content || \"\";\nlet parsed = {};\ntry {\n  const match = String(raw).match(/\\{[\\s\\S]*\\}/);\n  parsed = match ? JSON.parse(match[0]) : {};\n} catch (error) {\n  parsed = {};\n}\nconst message = parsed.message || parsed.reply || parsed.emailBody || parsed.body || parsed.summary || String(raw);\nreturn { ...input, ...parsed, responseText: String(raw), message };",
          "timeout": 5000
        },
        "notes": {
          "family": "Data",
          "what": "Writes and runs any JavaScript you like to transform or process your data",
          "overview": "Run sandboxed JavaScript once against the current workflow data and return the transformed value for downstream nodes.",
          "tips": [
            "Always return the value the next node should receive",
            "Default timeout is 5000ms and runtime caps at 30000ms",
            "Use Output Schema such as {\"type\":\"object\"} only as a top-level shape hint",
            "JavaScript has no credentials; connect downstream service node accounts separately",
            "Do not paste API keys, tokens, or passwords into code"
          ],
          "docsHref": "/docs/nodes/javascript",
          "why": "Turns the agent response into top-level fields before any branch or output node reads it."
        }
      }
    },
    {
      "id": "pipedrive_log_note",
      "type": "custom",
      "position": {
        "x": 1380,
        "y": 300
      },
      "data": {
        "icon": "CircleDollarSign",
        "type": "pipedrive",
        "label": "Log Sales Note",
        "category": "crm",
        "config": {
          "resource": "deals",
          "operation": "update",
          "id": "{{input.dealId}}",
          "data": {
            "note": "{{input.summary}}"
          }
        },
        "notes": {
          "family": "CRM",
          "what": "Manages deals, contacts, and activities in Pipedrive sales CRM",
          "overview": "Important: this node supports 11 real Pipedrive resources (person, organization, deal, activity, note, pipeline, stage, product, lead, file, webhook), each using its own individually-named ID and create/update fields - but the visual panel's generic Resource ID, Data (JSON), Search Term, and Fields inputs are never read by any of them. The real fields (like personId, dealTitle, searchTerm) must currently be set via workflow JSON or an AI-generated workflow. The \"Get Many\" dropdown option also never works - the real value is \"list\". Company Domain is fully decorative; the API client always calls the single global api.pipedrive.com address using only the API Token.",
          "tips": [],
          "docsHref": "/docs/nodes/pipedrive",
          "why": "Logs the sales conversation in the CRM as a guaranteed downstream write."
        }
      }
    },
    {
      "id": "slack_sales_notify",
      "type": "custom",
      "position": {
        "x": 1700,
        "y": 300
      },
      "data": {
        "icon": "Hash",
        "type": "slack_message",
        "label": "Notify Sales",
        "category": "output",
        "config": {
          "channel": "#sales",
          "message": "Sales agent handled {{input.email}}: {{input.summary}}"
        },
        "notes": {
          "family": "Communication",
          "what": "Sends messages using a Slack app or bot connection",
          "overview": "Send Slack bot messages to channels, direct messages, or existing threads through a saved Slack OAuth2 connection. Use it for workplace alerts, support replies, incident updates, approvals, report summaries, and deployment notifications.",
          "tips": [
            "Select a Slack OAuth2 connection; do not paste bot tokens into message fields",
            "The Slack app needs chat:write, and the bot must be invited before it can post in private channels",
            "Use {{$json.channelId}} and {{$json.threadTs}} from Slack Trigger to reply in the same conversation",
            "Keep Message filled as readable fallback text even when Blocks contains a rich Block Kit layout",
            "Connect downstream service accounts separately; Slack OAuth2 only authorizes the Slack send"
          ],
          "docsHref": "/docs/nodes/slack_message",
          "why": "Alerts sales that the conversation was qualified and logged."
        }
      }
    }
  ],
  "edges": [
    {
      "id": "e_1_webhook_intake_memory_context",
      "source": "webhook_intake",
      "target": "memory_context"
    },
    {
      "id": "e_2_memory_context_ai_agent",
      "source": "memory_context",
      "target": "ai_agent",
      "targetHandle": "memory"
    },
    {
      "id": "e_3_webhook_intake_tool_pipedrive_lookup",
      "source": "webhook_intake",
      "target": "tool_pipedrive_lookup"
    },
    {
      "id": "e_4_tool_pipedrive_lookup_ai_agent",
      "source": "tool_pipedrive_lookup",
      "target": "ai_agent",
      "targetHandle": "tool"
    },
    {
      "id": "e_5_webhook_intake_tool_calendar_slots",
      "source": "webhook_intake",
      "target": "tool_calendar_slots"
    },
    {
      "id": "e_6_tool_calendar_slots_ai_agent",
      "source": "tool_calendar_slots",
      "target": "ai_agent",
      "targetHandle": "tool"
    },
    {
      "id": "e_7_webhook_intake_ai_agent",
      "source": "webhook_intake",
      "target": "ai_agent",
      "targetHandle": "userInput"
    },
    {
      "id": "e_8_ai_agent_js_parse_agent",
      "source": "ai_agent",
      "target": "js_parse_agent"
    },
    {
      "id": "e_9_js_parse_agent_pipedrive_log_note",
      "source": "js_parse_agent",
      "target": "pipedrive_log_note"
    },
    {
      "id": "e_10_pipedrive_log_note_slack_sales_notify",
      "source": "pipedrive_log_note",
      "target": "slack_sales_notify"
    }
  ],
  "difficulty": "Intermediate",
  "estimated_setup_time": 20,
  "tags": [
    "sales",
    "pipedrive",
    "calendar",
    "webhook",
    "ai-agent"
  ],
  "is_featured": false,
  "is_active": true,
  "use_count": 0,
  "version": 2
}
