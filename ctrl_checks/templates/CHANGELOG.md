# Template Library v2 — what changed and why

Generated by `apply-fixes.cjs`. Baseline: `snapshot/live-2026-07-31.json`
(what was live in production on 2026-07-31).

**242 changes across 36 templates.**

## Approval Chance Predictor

- **FIX** — airtable_lookup_readiness: unfiltered read fed every client in the table into a single prediction prompt
- **FIX** — ai_predict_chance: {{input.record}} -> {{input.records}} (singular key never existed)
- **FIX** — airtable_save_prediction: Airtable emits records[].id, never recordId — the prediction was never saved
- **FIX** — airtable_lookup_readiness: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — airtable_save_prediction: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Business Details Matcher

- **FIX** — airtable_read_ready_clients: replaces the phantom lastSeenIds diff: the filter itself excludes already-checked clients, so each poll takes exactly one unchecked client
- **FIX** — js_detect_newly_ready: now selects one client rather than diffing against state that never persisted
- **FIX** — js_detect_newly_ready: lastSeenIds never persisted between scheduled runs, so every client looked new on every poll and was re-emailed every 15 minutes
- **FIX** — ai_extract_details: asked for labelled prose that was then regex-scanned for the word "mismatch" — now returns parseable JSON
- **FIX** — js_compare_versions: replaced the /mismatch|does not match|differ/i prose regex with a real field-by-field comparison
- **FIX** — airtable_write_match_results: record id now comes from the selected client, not a key that never existed
- **ADD** — airtable_mark_checked (airtable) between airtable_write_match_results and gmail_send_match_report: without a marker the same client matched the filter forever and was re-emailed every 15 minutes
- **ADD** — js_stamp_checked (javascript) between js_compare_versions and airtable_write_match_results: supplies a real timestamp because the resolver does not implement {{$now}}
- **ADD** — if_hasclient (if_else) after js_detect_newly_ready: most 15-minute polls find nothing; without this the workflow fetched a blank URL and emailed an empty report every run
- **FIX** — description: now describes the one-per-run behaviour accurately
- **FIX** — airtable_read_ready_clients: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — http_fetch_website_details: family "data" -> "http_api" (per the note registry, where http_request is filed under "HTTP & API")
- **FIX** — airtable_write_match_results: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — airtable_mark_checked: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Client Priority Ranker

- **FIX** — airtable_read_client_history: bounded so a large client table cannot blow the prompt context
- **FIX** — js_sort_by_priority: emitted an object array that Slack rendered as a raw JSON blob
- **FIX** — slack_send_ranked_list: renders the formatted list instead of a JSON blob
- **FIX** — category: moved to "Sales, Support & Internal Operations" sector
- **FIX** — airtable_read_client_history: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Clinic Appointment Reminder

- **ADD** — new Healthcare & Clinics template with 6 working-node steps
- **FIX** — airtable_read_appointment: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — airtable_mark_appointment: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## CRM Data Cleanup Assistant

- **ADD** — new Sales, Support & Internal Operations template with 6 working-node steps

## Cross-Platform Sync Engine

- **FIX** — airtable_read_all_clients: the phantom lastSnapshot made every record look changed on every poll, so the whole table was re-pushed to HubSpot and Sheets every 15 minutes forever. Now only genuinely-modified rows match, one per run.
- **FIX** — js_diff_changed_fields: selects one modified record instead of diffing against state that never persisted
- **FIX** — js_diff_changed_fields: hubspotContactId and diffFields lived inside changed[i] and were read as top-level scalars, so both were always blank
- **FIX** — sheets_update_shared_copy: spreadsheetId and range were both blank required fields, and values pointed at an array of objects Sheets cannot write
- **ADD** — airtable_mark_synced (airtable) between sheets_update_shared_copy and slack_log_sync: stamps the sync time so the read filter stops re-selecting the same row — this is what ends the re-push storm
- **FIX** — slack_log_sync: referenced {{input.changed.length}}, which no longer exists in the one-per-run design
- **ADD** — if_hasrecord (if_else) after js_diff_changed_fields: without this an empty poll still pushed a blank contact to HubSpot and a blank row to Sheets — every 15 minutes
- **FIX** — description: documents the one-per-run behaviour and the HubSpotId prerequisite
- **FIX** — category: moved to "Sales, Support & Internal Operations" sector
- **FIX** — airtable_read_all_clients: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — hubspot_push_changes: family "database" -> "crm" (per the note registry, where hubspot is filed under "CRM")
- **FIX** — sheets_update_shared_copy: family "database" -> "data" (per the note registry, where google_sheets is filed under "Data")
- **FIX** — airtable_mark_synced: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Customer Churn Risk Alert

- **ADD** — new Sales, Support & Internal Operations template with 7 working-node steps
- **FIX** — airtable_read_customer: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — airtable_mark_churn_checked: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Customer Support Agent

- **FIX** — switch_intent_1: the registry requires `expression` (not `value`) and object cases — both were off-schema
- **FIX** — slack_escalate: blank required field with no indication anything was needed
- **FIX** — if_confidence_check: converted from the legacy condition string to the canonical conditions[] form
- **FIX** — js_intent_1: reads correctly whether chat_trigger emits a string or an object
- **FIX** — tags: removed "production-ready"
- **FIX** — db_order_lookup: database_read -> postgresql. database_read/database_write are missing from the frontend catalog, so this node was silently rewritten to a generic http_request on every copy
- **FIX** — js_format_order: read input.data[0]; the PostgreSQL node returns rows[]
- **FIX** — db_escalate: database_write -> postgresql. database_read/database_write are missing from the frontend catalog, so this node was silently rewritten to a generic http_request on every copy
- **FIX** — category: moved to "Sales, Support & Internal Operations" sector
- **FIX** — db_order_lookup: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")
- **FIX** — log_output_1: family "output" -> "utility" (per the note registry, where log_output is filed under "Utility")
- **FIX** — db_escalate: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")
- **FIX** — log_escalated: family "output" -> "utility" (per the note registry, where log_output is filed under "Utility")

## Document Vault — Intake & Auto-Tagging

- **SPLIT** — the original template held two triggers and two fully disconnected subgraphs in one graph, which violates the DAG rule (one trigger, no orphans) and would fail save-validation
- **ADD** — js_parse_doc_tags (javascript) between ai_tag_document and airtable_store_tagged_doc: Gemini was told to return JSON but nothing parsed it, so DocumentType and DocumentDate were written blank
- **FIX** — airtable_store_tagged_doc: FileLink read {{input.fileUrl}}, which the form node never produces — the file field is keyed "document"
- **FIX** — airtable_store_tagged_doc: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Document Vault — Smart Search

- **FIX** — js_parse_search_query: read input.message directly; now reads correctly whether chat_trigger emits a string or an object
- **FIX** — airtable_search_vault: bounded so a large vault cannot blow the agent prompt context
- **FIX** — whatsapp_reply_search: whatsapp -> chat_send. WhatsApp is not currently verified working; this workflow starts from chat, so the answer should return to the chat session.
- **FIX** — airtable_search_vault: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Employee Onboarding Checklist

- **ADD** — new Sales, Support & Internal Operations template with 7 working-node steps
- **FIX** — airtable_read_new_hire: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — airtable_mark_onboarding: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Expense Policy Checker

- **ADD** — new Finance, Accounting & Insurance template with 6 working-node steps
- **FIX** — postgres_save_expense: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")

## FAQ Answering Assistant

- **FIX** — airtable_read_faq: the entire FAQ table was being pushed into every prompt
- **ADD** — js_parse_faq_answer (javascript) between ai_answer_from_faq and if_confidence_check: the AI was told to return JSON but nothing parsed it, so $json.confidence never existed, the gate was always false, and every client question escalated to Slack instead of being answered
- **FIX** — whatsapp_reply_client: whatsapp -> chat_send. WhatsApp is not currently verified working; confident FAQ answers should return to the active chat session.
- **FIX** — tags: replaced "whatsapp" with "chat"
- **FIX** — category: moved to "Sales, Support & Internal Operations" sector
- **FIX** — airtable_read_faq: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Finance / Compliance Agent

- **FIX** — js_compliance_check: the $10,000 approval threshold was hardcoded in JS — the single most likely thing a customer changes. Now reads input.approvalThreshold with the same default.
- **FIX** — js_format_alert: now emits violationsJson, because the resolver cannot evaluate {{JSON.stringify(...)}}
- **FIX** — db_save_flag: {{JSON.stringify(input.violations)}} is not a syntax the resolver supports — it wrote the literal string into the violations column
- **FIX** — email_finance_alert: hardcoded finance@company.com silently emailed a non-existent address
- **FIX** — slack_finance_alert: blank required field
- **FIX** — if_compliant_check: converted from the legacy condition string to the canonical conditions[] form
- **FIX** — tags: removed "production-ready"
- **FIX** — db_save_transaction: database_write -> postgresql. database_read/database_write are missing from the frontend catalog, so this node was silently rewritten to a generic http_request on every copy
- **FIX** — db_save_flag: database_write -> postgresql. database_read/database_write are missing from the frontend catalog, so this node was silently rewritten to a generic http_request on every copy
- **FIX** — category: moved to "Finance, Accounting & Insurance" sector
- **FIX** — db_save_transaction: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")
- **FIX** — log_approved: family "output" -> "utility" (per the note registry, where log_output is filed under "Utility")
- **FIX** — db_save_flag: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")
- **FIX** — email_finance_alert: family "google" -> "output" (per the note registry, where google_gmail is filed under "Communication")
- **FIX** — log_flagged: family "output" -> "utility" (per the note registry, where log_output is filed under "Utility")

## HR / Hiring Workflow Agent

- **FIX** — email_rejection: sent automated rejection emails straight to candidates with no human review — now routed to the recruiter as a draft. This is a legal and brand exposure, not a style preference.
- **FIX** — email_rejection: reflects that a human now sends the rejection
- **FIX** — js_generate_calendar: hardcoded https://calendly.com/hr-team/interview
- **FIX** — if_shortlist_check: converted from the legacy condition string to the canonical conditions[] form
- **FIX** — tags: removed "production-ready"
- **FIX** — db_shortlist: database_write -> postgresql. database_read/database_write are missing from the frontend catalog, so this node was silently rewritten to a generic http_request on every copy
- **FIX** — db_reject: database_write -> postgresql. database_read/database_write are missing from the frontend catalog, so this node was silently rewritten to a generic http_request on every copy
- **FIX** — category: moved to "Sales, Support & Internal Operations" sector
- **FIX** — db_shortlist: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")
- **FIX** — email_interview: family "google" -> "output" (per the note registry, where google_gmail is filed under "Communication")
- **FIX** — db_reject: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")
- **FIX** — email_rejection: family "google" -> "output" (per the note registry, where google_gmail is filed under "Communication")
- **FIX** — log_processing: family "output" -> "utility" (per the note registry, where log_output is filed under "Utility")

## Insurance Pre-Authorization Tracker

- **ADD** — new Healthcare & Clinics template with 8 working-node steps
- **FIX** — airtable_read_preauth: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — airtable_mark_preauth: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Internal Knowledge / Ops Agent

- **FIX** — js_prepare_query: read input.message directly; now reads correctly whether chat_trigger emits a string or an object
- **FIX** — db_search_kb: orderBy: "relevance" is not a column — it was silently ignored while the node returned 5 arbitrary rows regardless of the question
- **FIX** — js_format_kb_results: now emits contentLength so the found/non-empty check can be expressed as two canonical conditions
- **FIX** — if_kb_found: THE core fix: "{{input.found}} === true && {{input.content}}.length > 0" parsed to field=input.found, operator=equals, value="true && {{input.content}}.length > 0" — a permanently false comparison. Every employee question took the fallback branch, so the knowledge base was never read from and the knowledge team was paged for every single question.
- **FIX** — slack_escalate_kb: blank required field
- **FIX** — tags: removed "production-ready"
- **FIX** — db_search_kb: database_read -> postgresql. database_read/database_write are missing from the frontend catalog, so this node was silently rewritten to a generic http_request on every copy. It also had no search term at all (filters "{}" and a non-existent "relevance" column), so it returned five arbitrary rows regardless of the question — the query now actually searches
- **FIX** — js_format_kb_results: read input.data; the PostgreSQL node returns rows[]
- **FIX** — db_log_query: database_write -> postgresql. database_read/database_write are missing from the frontend catalog, so this node was silently rewritten to a generic http_request on every copy
- **FIX** — db_log_not_found: database_write -> postgresql. database_read/database_write are missing from the frontend catalog, so this node was silently rewritten to a generic http_request on every copy
- **FIX** — category: moved to "Sales, Support & Internal Operations" sector
- **FIX** — db_search_kb: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")
- **FIX** — db_log_query: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")
- **FIX** — db_log_not_found: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")
- **FIX** — log_output_kb: family "output" -> "utility" (per the note registry, where log_output is filed under "Utility")

## Invoice Approval Triage

- **ADD** — new Finance, Accounting & Insurance template with 7 working-node steps
- **FIX** — postgres_log_invoice: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")

## Lab Report Follow-up

- **ADD** — new Healthcare & Clinics template with 6 working-node steps

## License Renewal Reminder

- **FIX** — schedule_daily_check: was daily; now every 10 minutes because each run handles one licence (144/day drains any realistic queue)
- **FIX** — schedule_daily_check: reflects the one-per-run design
- **FIX** — airtable_read_expiry_dates: reads one licence at a time and skips any already reminded at its current milestone — the old version re-sent every single day for 90 days
- **FIX** — js_flag_expiring: selects one licence instead of returning an array nothing could iterate
- **FIX** — js_flag_expiring: returned a flagged[] array that both output nodes then read as scalars, so every placeholder was blank and no usable reminder was ever sent
- **ADD** — airtable_mark_reminded (airtable) between js_flag_expiring and whatsapp_send_reminder: records which milestone was sent so the same licence is not reminded again until the next milestone
- **FIX** — gmail_send_backup_reminder: sourced after the marker so the reminder fields are in scope
- **ADD** — if_shouldremind (if_else) after js_flag_expiring: runs every 10 minutes and usually finds nothing; without this it WhatsApped and emailed blank reminders on every empty poll
- **FIX** — js_flag_expiring: removed unused WhatsApp number output
- **DEL** — whatsapp_send_reminder: WhatsApp is not currently verified working; the verified Gmail node now carries the client reminder.
- **FIX** — gmail_send_backup_reminder: this is now the primary verified reminder channel
- **FIX** — gmail_send_backup_reminder: removed backup/WhatsApp wording because this email is now the primary reminder
- **FIX** — description: changed the promised output from WhatsApp plus email to verified email reminders
- **FIX** — tags: replaced "whatsapp" with "email"
- **FIX** — airtable_read_expiry_dates: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — airtable_mark_reminded: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Live Status Lookup Bot

- **FIX** — js_parse_lookup: read input.message directly; now reads correctly whether chat_trigger emits a string or an object
- **FIX** — airtable_lookup_status: the read was unfiltered and js_format_reply always took records[0], so the bot returned the same client no matter what was asked
- **FIX** — js_format_reply: added an explicit not-found reply instead of rendering "not found" inside an otherwise normal-looking status line
- **FIX** — airtable_lookup_status: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Meeting Notes to Action Items

- **ADD** — new Sales, Support & Internal Operations template with 5 working-node steps
- **FIX** — postgres_save_meeting: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")

## Missing Document Finder

- **FIX** — form_doc_upload: clientType was a select with no options, yet js_check_missing branches on exactly Individual/Business
- **FIX** — airtable_log_document: dropped ReceivedAt: {{$now}} — the resolver has no $now, so the literal string was written to Airtable (use an Airtable "Created time" field instead)
- **ADD** — airtable_read_prior_docs (airtable) between airtable_log_document and js_check_missing: nothing read previously-received documents, so the checklist only ever saw the one file just uploaded and reported everything else missing
- **FIX** — js_check_missing: read input.receivedTypes, which nothing produced — now derived from the client's full document history
- **FIX** — form_doc_upload: collects email instead of WhatsApp because Gmail is verified working and WhatsApp is not
- **FIX** — ai_write_missing_message: writes an email-friendly chase message instead of WhatsApp copy
- **FIX** — whatsapp_send_missing: whatsapp -> google_gmail. WhatsApp is not currently verified working; email is a verified client notification channel.
- **FIX** — description: changed the promised output from WhatsApp to email
- **FIX** — tags: replaced "whatsapp" with "email"
- **FIX** — airtable_log_document: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — airtable_read_prior_docs: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## New Client Risk Checker

- **FIX** — http_domain_age_lookup: {{$credentials.whoisxml.apiKey}} is not a syntax the resolver implements, and whoisxml is not a registered credential provider — replaced with an explicit placeholder
- **FIX** — if_high_risk: already canonical; normalised for consistency across the library
- **FIX** — http_domain_age_lookup: family "data" -> "http_api" (per the note registry, where http_request is filed under "HTTP & API")
- **FIX** — airtable_log_risk: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Overdue Task Tracker

- **FIX** — schedule_hourly_check: was hourly; now every 5 minutes because each run handles one overdue request
- **FIX** — schedule_hourly_check: reflects the one-per-run design
- **FIX** — airtable_read_open_requests: one request per run, and re-alerts are rate-limited to once every 4 hours instead of firing every single hour forever
- **FIX** — js_flag_overdue: selects one request instead of returning an array nothing could iterate
- **FIX** — js_flag_overdue: returned flagged[] read downstream as scalars; the owner/manager Slack channels were referenced but produced by nothing — they now come from the Airtable row with a safe default
- **FIX** — if_very_overdue: normalised to the canonical conditions[] form
- **FIX** — if_very_overdue: branched off the Slack node's output, where isVeryOverdue no longer exists — now reads from the node that computes it
- **ADD** — airtable_mark_alerted (airtable) after slack_alert_owner: stamps the alert time so the 4-hour re-alert window in the read filter works
- **ADD** — if_isoverdue (if_else) after js_flag_overdue: runs every 5 minutes and usually finds nothing; without this it posted a blank Slack alert on every empty poll
- **FIX** — category: moved to "Sales, Support & Internal Operations" sector
- **FIX** — airtable_read_open_requests: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — airtable_mark_alerted: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Patient Feedback Classifier

- **ADD** — new Healthcare & Clinics template with 6 working-node steps
- **FIX** — airtable_log_feedback: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Patient Intake Triage

- **ADD** — new Healthcare & Clinics template with 6 working-node steps

## Payment Failure Recovery

- **ADD** — new Finance, Accounting & Insurance template with 6 working-node steps

## Sales & Lead Qualification Agent

- **DEL** — ai_qualify_questions: generated three qualifying questions that nothing downstream ever read — one wasted LLM call per lead
- **DEL** — js_parse_questions: parsed the questions from the deleted node into a field nothing referenced
- **FIX** — ai_score_lead: the prompt contained no {{input.*}} at all — the model never saw the lead, so every score was invented
- **FIX** — js_generate_booking: hardcoded https://calendly.com/your-team/meeting — a literal placeholder that 404s for every customer
- **FIX** — if_qualified_check: converted from the legacy condition string to the canonical conditions[] form
- **FIX** — tags: removed "production-ready"
- **FIX** — db_create_crm: database_write -> hubspot. A qualified lead belongs in your CRM, not in a generic "leads" table written through a raw database node. HubSpot is a real CRM node present in both catalogs.
- **FIX** — category: moved to "Sales, Support & Internal Operations" sector
- **FIX** — db_create_crm: family "database" -> "crm" (per the note registry, where hubspot is filed under "CRM")
- **FIX** — email_qualified: family "google" -> "output" (per the note registry, where google_gmail is filed under "Communication")
- **FIX** — email_followup: family "google" -> "output" (per the note registry, where google_gmail is filed under "Communication")
- **FIX** — log_result: family "output" -> "utility" (per the note registry, where log_output is filed under "Utility")

## Smart Alert Co-Pilot

- **DEL** — airtable_read_related_context: read the same table with the same (empty) filter as the node before it, returning identical rows — a redundant Airtable call per poll
- **FIX** — airtable_read_all_records: the phantom lastSnapshot made every record look changed on every poll; now only genuinely-modified rows match, one per run
- **FIX** — js_diff_for_alerts: selects one modified record instead of diffing against state that never persisted
- **FIX** — js_diff_for_alerts: lastSnapshot never persisted between runs
- **ADD** — js_parse_alert_decision (javascript) after ai_agent_judge_alert: js_route_notification did input.response.severity on what the agent returns as text, so severity was always "low"
- **ADD** — if_alert_worthy (if_else) between js_parse_alert_decision and js_route_notification: THE core fix: WhatsApp fired on every polled change regardless of the AI's verdict, so the template was a noisier dumb alert. The gate is what the whole template is for.
- **FIX** — js_route_notification: complianceLeadPhone and accountManagerPhone were referenced but produced by nothing — now explicit placeholders
- **ADD** — airtable_mark_alert_checked (airtable) between js_parse_alert_decision and if_alert_worthy: stamps every judged record before the gate, so records judged not-alert-worthy are not re-judged (and re-billed) on every subsequent poll
- **ADD** — if_hasrecord (if_else) after js_diff_for_alerts: without this an empty 10-minute poll still burned an AI agent call and sent a blank WhatsApp alert
- **FIX** — js_route_notification: routes to Slack channels because Slack is verified working and WhatsApp is not
- **FIX** — whatsapp_send_smart_alert: whatsapp -> slack_message. WhatsApp is not currently verified working; Slack is a verified internal alert channel.
- **FIX** — tags: replaced "whatsapp" with "slack"
- **FIX** — airtable_read_all_records: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — airtable_mark_alert_checked: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Submission Package Builder

- **FIX** — airtable_read_ready_status: one ready client per run; the filter excludes clients whose package has already been sent
- **FIX** — js_detect_newly_ready_status: selects one client rather than diffing against state that never persisted
- **FIX** — js_detect_newly_ready_status: lastSeenIds never persisted, so every ready client was re-packaged every 15 minutes
- **FIX** — http_screenshot_pages: {{$credentials.screenshotone.apiKey}} is not a syntax the resolver implements, and screenshotone is not a registered credential provider — replaced with an explicit placeholder the user edits
- **ADD** — merge_package (merge) before drive_list_existing_docs: the screenshot and policy-text branches had no join, so the email could never see both
- **ADD** — airtable_mark_package_sent (airtable) between drive_list_existing_docs and gmail_send_package: without a marker the same client was re-packaged and re-emailed every 15 minutes
- **ADD** — if_hasclient (if_else) after js_detect_newly_ready_status: most polls find nothing; without this the workflow screenshotted a blank URL and emailed an empty package every 15 minutes
- **FIX** — airtable_read_ready_status: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — http_screenshot_pages: family "data" -> "http_api" (per the note registry, where http_request is filed under "HTTP & API")
- **FIX** — http_fetch_policy_text: family "data" -> "http_api" (per the note registry, where http_request is filed under "HTTP & API")
- **FIX** — drive_list_existing_docs: family "database" -> "data" (per the note registry, where google_drive is filed under "Data")
- **FIX** — airtable_mark_package_sent: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Support Ticket Triage

- **ADD** — new Sales, Support & Internal Operations template with 7 working-node steps
- **FIX** — postgres_create_ticket: family "database" -> "data" (per the note registry, where postgresql is filed under "Data")

## Vendor Due Diligence

- **ADD** — new Finance, Accounting & Insurance template with 7 working-node steps
- **FIX** — airtable_log_vendor: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Verification Co-Pilot Chat

- **FIX** — js_parse_query: read input.message directly; now reads correctly whether chat_trigger emits a string or an object
- **FIX** — airtable_lookup_client_record: the whole client table was being handed to the AI on every question
- **DEL** — http_status_api_optional: labelled "(Optional)" but sat inline with url {{input.statusApiUrl}}, which nothing defines — a failure here killed the whole run
- **FIX** — ai_agent_answer: dropped the reference to the deleted status-API node
- **FIX** — whatsapp_reply_copilot: whatsapp -> chat_send. WhatsApp is not currently verified working; this chat workflow should answer in the same chat channel.
- **FIX** — description: removed WhatsApp dependency from the user-facing copy
- **FIX** — tags: replaced "whatsapp" with "chat"
- **FIX** — airtable_lookup_client_record: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Verification Readiness Checker

- **ADD** — js_compose_results (javascript) after ai_fix_list: gmail referenced {{input.fixList}}, which no node produced — the customer email shipped with a blank fix list
- **FIX** — airtable_update_score: Airtable emits records[].id, never recordId — the score was never written back
- **FIX** — whatsapp_send_results: now sourced from the assembled payload so {{input.score}} resolves
- **FIX** — form_intake: removes the WhatsApp number field because no working output node now uses it
- **FIX** — airtable_create_client: stops storing a WhatsApp field that the working-node template no longer collects
- **FIX** — js_compose_results: assembles only fields consumed by the remaining verified Gmail output
- **DEL** — whatsapp_send_results: WhatsApp is not currently verified working; the full readiness result is already delivered by Gmail.
- **FIX** — description: changed the promised output from email plus WhatsApp to email
- **FIX** — tags: replaced "whatsapp" with "email"
- **FIX** — airtable_create_client: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
- **FIX** — http_fetch_website: family "data" -> "http_api" (per the note registry, where http_request is filed under "HTTP & API")
- **FIX** — airtable_update_score: family "database" -> "data" (per the note registry, where airtable is filed under "Data")

## Weekly Pipeline Report

- **ADD** — new Sales, Support & Internal Operations template with 5 working-node steps
- **FIX** — airtable_read_pipeline: family "database" -> "data" (per the note registry, where airtable is filed under "Data")
